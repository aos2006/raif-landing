import { combineReducers } from 'redux';
import News from 'modules/News';
import { Login } from 'modules/User';

export default combineReducers({
  // [Login.types.NAME]: Login.reducer,
  // [News.types.NAME]: News.reducer,
});
